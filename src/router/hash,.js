const router = {
    renderView() {
        let hash = location.hash;
        switch (hash) {
            case '#home':
                $('#app').html('');
                break;
            case '#home':
                $('#app').html('');
                break;

            case '#home':
                $('#app').html('');
                break;
        }
        
    }
}